'use strict';

app
    .constant('websiteConfig', {
        TITLE:'BiblioToon',
        DOMAIN:'fr',
        DATE_FORMAT:'dd/MM/yyyy'
    })

    .constant('dbConfig', {
        DB_NAME:'dbTest'
    })
;
